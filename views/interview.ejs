<%- include('partials/header') %>

    <head>
      <link rel="stylesheet" href="/blog.css">
    </head>

    <div>
      


        <div class="container p-5">
          <div class="row">
            

            <% for(let i in Interview){ %>
            <% if (Interview[i].type=="Interview") {%>
            <div class="col-12 col-sm-8 col-md-6 col-lg-4">



              <div class="card">
                <img class="card-img" src="https://i0.wp.com/www.iedunote.com/img/21161/interview-scaled.jpg"
                  alt="Bologna">
                <div class="card">
                  <a href="#" class="btn btn-light btn-sm"><%= Interview[i].type %></a>
                </div>


                <div class="card-body">
                  <h4 class="card-title"><%= Interview[i].title %></h4>
                  <small class="text-muted cat">
                    <i class="far fa-clock text-info"></i> 30 minutes
                  </small>

                  <% if(Interview[i].content.length>25){%>
                    <%    str1=Interview[i].content;%>
                    <%     str1=str1.substring(0,100);%>
                      <p class="card-text"><%= str1 %> . . . </p>
                    <% } else{%>
                      <%     str1=Interview[i].content; %>
                      <p class="card-text"><%= str1 %></p>
                    <% }%>
                      <a href="/content/?id=<%= i %>" class="btn btn-info">Read More</a>
                </div>
                
                <div class="card-footer text-muted d-flex justify-content-between bg-transparent border-top-0">
                  <div class="views">Apr 1, 12:45PM
                  </div>

                  <div class="stats">
                    <i class="fa-solid fa-heart"><%= Interview[i].upvote %></i>
                    <!-- <i class="fa-solid fa-heart-crack"></i> 12 -->
                  </div>

                </div>
              </div>
            </div>
            <% } %>
            <% } %>
          </div>

        </div>
      </div>

      <%- include('partials/footer') %>
      </body>

      </html>